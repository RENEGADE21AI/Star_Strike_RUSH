{
  "status": "superseded",
  "summary": "This historical point-in-time audit no longer describes the active architecture. Current browser writes are denied, device-local progression is authoritative, and Firebase is account identity plus legacy archive only.",
  "currentBoundaries": [
    "Firestore rules are default-deny and browser progression writes are denied.",
    "submitRunReceipt and joinWeeklyLeague reject before authentication or Firestore access.",
    "claimSeasonReward is an inert retired compatibility endpoint with no reward calculation, authentication, reads, or writes.",
    "Season XP, tiers, reward lanes, and claims are not active progression.",
    "Historical season_reward_claims documents remain owner-readable and browser-write-denied for audit preservation.",
    "The executable Firestore emulator and release-integrity suites are the current verification evidence."
  ]
}
