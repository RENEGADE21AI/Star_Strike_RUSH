# Source artwork

Original, unoptimized artwork supplied for Star Strike RUSH is preserved here.
Firebase Hosting ignores this directory; the playable build loads only the
cropped, transparent, browser-sized derivatives in `assets/sprites/` and
`assets/ui/`. Rebuild derivatives with `scripts/import_user_art.py`.
